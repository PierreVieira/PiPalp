/*
* A classe coordenada serve de base para os desenhos dos leds.
* Pois com essa informação sabemos o ponto de origem do desenho de cada LED.*/

class Coordenada{
  double x, y;
  Coordenada(this.x, this.y);
}